info:
  name: create_item
  type: graphql
  seq: 1

graphql:
  method: POST
  url: "{{base_url}}/graphql"
  auth: inherit
  body:
    query: |-
      mutation {
        create_articles_item(data: { title: "New Article", status: "draft" }) {
          id
          title
        }
      }

settings:
  encodeUrl: true

docs: |-
  Create a single item in a collection via GraphQL mutation.
  https://directus.io/docs/api/graphql
