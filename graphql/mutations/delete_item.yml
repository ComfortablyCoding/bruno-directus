info:
  name: delete_item
  type: graphql
  seq: 5

graphql:
  method: POST
  url: "{{base_url}}/graphql"
  auth: inherit
  body:
    query: |-
      mutation DeleteArticle($id: ID!) {
        delete_articles_item(id: $id) {
          id
        }
      }
    variables: |-
      {
        "id": "1"
      }

settings:
  encodeUrl: true

docs: |-
  Delete a single item from a collection via GraphQL mutation.
  https://directus.io/docs/api/graphql
