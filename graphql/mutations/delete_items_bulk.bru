meta {
  name: delete_items_bulk
  type: graphql
  seq: 6
}

post {
  url: {{base_url}}/graphql
  body: graphql
  auth: inherit
}

body:graphql {
  mutation {
    delete_articles_items(ids: ["1", "2"]) {
      ids
    }
  }
}

docs {
  Delete multiple items by ID from a collection via GraphQL mutation.
  https://directus.io/docs/api/graphql
}
