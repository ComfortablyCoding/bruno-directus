info:
  name: query_collection_by_id
  type: http
  seq: 2

http:
  method: GET
  url: "{{base_url}}/collections/:collection"
  params:
    - name: collection
      value: "my_collection"
      type: path
  auth: inherit

settings:
  encodeUrl: true

docs: |-
  Retrieve a single collection by its name.
  https://directus.io/docs/api/collections
