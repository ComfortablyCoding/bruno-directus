info:
  name: delete_collection
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{base_url}}/collections/:collection"
  params:
    - name: collection
      value: "my_collection"
      type: path
  auth: inherit

settings:
  encodeUrl: true

docs: |-
  Delete an existing collection and all its data by name.
  https://directus.io/docs/api/collections
