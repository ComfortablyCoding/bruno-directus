info:
  name: update_operation
  type: http
  seq: 5

http:
  method: PATCH
  url: "{{base_url}}/operations/:id"
  params:
    - name: id
      value: "operation-id"
      type: path
  body:
    type: json
    data: |-
      {
        "options": {"message": "Updated message"}
      }
  auth: inherit

settings:
  encodeUrl: true

docs: |-
  Update a single flow operation by its primary key.
  https://directus.io/docs/api/operations
