meta {
  name: query_operation_by_id
  type: http
  seq: 2
}

get {
  url: {{base_url}}/operations/:id
  body: none
  auth: inherit
}

params:path {
  id: operation-id
}

params:query {
  ~fields: id,title,date_created
  ~filter: {"status":{"_eq":"published"}}
  ~sort: -date_created
  ~limit: 10
  ~offset: 0
  ~search: keyword
  ~meta: total_count,filter_count
}

docs {
  Retrieve a single flow operation by its primary key.
  https://directus.io/docs/api/operations
}
