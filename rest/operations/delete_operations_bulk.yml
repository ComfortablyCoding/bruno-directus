info:
  name: delete_operations_bulk
  type: http
  seq: 8

http:
  method: DELETE
  url: "{{base_url}}/operations"
  body:
    type: json
    data: |-
      ["id-1", "id-2"]
  auth: inherit

settings:
  encodeUrl: true

docs: |-
  Delete multiple flow operations by their primary keys.
  https://directus.io/docs/api/operations
