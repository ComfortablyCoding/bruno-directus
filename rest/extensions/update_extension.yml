info:
  name: update_extension
  type: http
  seq: 2

http:
  method: PATCH
  url: "{{base_url}}/extensions/:name"
  params:
    - name: name
      value: "my-extension"
      type: path
  body:
    type: json
    data: |-
      {
        "enabled": true
      }
  auth: inherit

settings:
  encodeUrl: true

docs: |-
  Enable or disable an extension by name.
  https://directus.io/docs/api/extensions
