meta {
  name: verify_hash
  type: http
  seq: 3
}

post {
  url: {{base_url}}/utils/hash/verify
  body: json
  auth: inherit
}

body:json {
  {
    "string": "my-secret-string",
    "hash": "$2b$10$..."
  }
}

docs {
  Verify a plain-text string against a bcrypt hash.
  https://directus.io/docs/api/utilities
}
