meta {
  name: sort_items
  type: http
  seq: 4
}

post {
  url: {{base_url}}/utils/sort/:collection
  body: json
  auth: inherit
}

params:path {
  collection: articles
}

body:json {
  {
    "item": "item-id",
    "to": "target-id"
  }
}

docs {
  Re-sort items in a collection using a manual sort field.
  https://directus.io/docs/api/utilities
}
