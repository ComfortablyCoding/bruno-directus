info:
  name: update_preset
  type: http
  seq: 5

http:
  method: PATCH
  url: "{{base_url}}/presets/:id"
  params:
    - name: id
      value: "preset-id"
      type: path
  body:
    type: json
    data: |-
      {
        "layout": "cards"
      }
  auth: inherit

settings:
  encodeUrl: true

docs: |-
  Update an existing preset by its primary key.
  https://directus.io/docs/api/presets
