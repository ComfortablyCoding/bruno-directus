info:
  name: create_preset
  type: http
  seq: 3

http:
  method: POST
  url: "{{base_url}}/presets"
  body:
    type: json
    data: |-
      {
        "collection": "articles",
        "layout": "tabular",
        "search": null
      }
  auth: inherit

settings:
  encodeUrl: true

docs: |-
  Create a new preset in the system.
  https://directus.io/docs/api/presets
