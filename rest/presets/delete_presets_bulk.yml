info:
  name: delete_presets_bulk
  type: http
  seq: 8

http:
  method: DELETE
  url: "{{base_url}}/presets"
  body:
    type: json
    data: |-
      ["id-1", "id-2"]
  auth: inherit

settings:
  encodeUrl: true

docs: |-
  Delete multiple presets at once.
  https://directus.io/docs/api/presets
