info:
  name: get_share_info
  type: http
  seq: 9

http:
  method: GET
  url: "{{base_url}}/shares/info/:id"
  params:
    - name: id
      value: "share-id"
      type: path
  auth:
    type: none

settings:
  encodeUrl: true

docs: |-
  Retrieve public metadata for a share without authentication.
  https://directus.io/docs/api/shares
