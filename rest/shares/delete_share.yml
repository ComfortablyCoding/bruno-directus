info:
  name: delete_share
  type: http
  seq: 7

http:
  method: DELETE
  url: "{{base_url}}/shares/:id"
  params:
    - name: id
      value: "share-id"
      type: path
  auth: inherit

settings:
  encodeUrl: true

docs: |-
  Delete a single share by its primary key.
  https://directus.io/docs/api/shares
