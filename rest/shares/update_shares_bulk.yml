info:
  name: update_shares_bulk
  type: http
  seq: 6

http:
  method: PATCH
  url: "{{base_url}}/shares"
  body:
    type: json
    data: |-
      {
        "keys": ["id-1", "id-2"],
        "data": {"max_uses": 20}
      }
  auth: inherit

settings:
  encodeUrl: true

docs: |-
  Update multiple shares by their primary keys.
  https://directus.io/docs/api/shares
