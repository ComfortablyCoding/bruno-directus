info:
  name: update_share
  type: http
  seq: 5

http:
  method: PATCH
  url: "{{base_url}}/shares/:id"
  params:
    - name: id
      value: "share-id"
      type: path
  body:
    type: json
    data: |-
      {
        "max_uses": 20
      }
  auth: inherit

settings:
  encodeUrl: true

docs: |-
  Update a single share by its primary key.
  https://directus.io/docs/api/shares
