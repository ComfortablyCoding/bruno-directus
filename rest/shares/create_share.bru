meta {
  name: create_share
  type: http
  seq: 3
}

post {
  url: {{base_url}}/shares
  body: json
  auth: inherit
}

body:json {
  {
    "collection": "articles",
    "item": "item-id",
    "max_uses": 10
  }
}

docs {
  Create a new share link for a specific item in a collection.
  https://directus.io/docs/api/shares
}
