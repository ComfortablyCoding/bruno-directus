info:
  name: create_share
  type: http
  seq: 3

http:
  method: POST
  url: "{{base_url}}/shares"
  body:
    type: json
    data: |-
      {
        "collection": "articles",
        "item": "item-id",
        "max_uses": 10
      }
  auth: inherit

settings:
  encodeUrl: true

docs: |-
  Create a new share link for a specific item in a collection.
  https://directus.io/docs/api/shares
