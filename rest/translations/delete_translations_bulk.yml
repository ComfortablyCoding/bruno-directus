info:
  name: delete_translations_bulk
  type: http
  seq: 8

http:
  method: DELETE
  url: "{{base_url}}/translations"
  body:
    type: json
    data: |-
      ["id-1", "id-2"]
  auth: inherit

settings:
  encodeUrl: true

docs: |-
  Delete multiple custom translation strings by their primary keys.
  https://directus.io/docs/api/translations
