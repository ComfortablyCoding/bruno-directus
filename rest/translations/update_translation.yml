info:
  name: update_translation
  type: http
  seq: 5

http:
  method: PATCH
  url: "{{base_url}}/translations/:id"
  params:
    - name: id
      value: "translation-id"
      type: path
  body:
    type: json
    data: |-
      {
        "value": "Updated welcome message"
      }
  auth: inherit

settings:
  encodeUrl: true

docs: |-
  Update a single custom translation string by its primary key.
  https://directus.io/docs/api/translations
