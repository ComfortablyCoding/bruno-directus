info:
  name: delete_comments_bulk
  type: http
  seq: 8

http:
  method: DELETE
  url: "{{base_url}}/comments"
  body:
    type: json
    data: |-
      ["id-1", "id-2"]
  auth: inherit

settings:
  encodeUrl: true

docs: |-
  Delete multiple comments at once.
  https://directus.io/docs/api/comments
