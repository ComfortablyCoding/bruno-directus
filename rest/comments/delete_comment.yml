info:
  name: delete_comment
  type: http
  seq: 7

http:
  method: DELETE
  url: "{{base_url}}/comments/:id"
  params:
    - name: id
      value: "comment-id"
      type: path
  auth: inherit

settings:
  encodeUrl: true

docs: |-
  Delete an existing comment by its primary key.
  https://directus.io/docs/api/comments
