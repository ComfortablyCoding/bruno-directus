info:
  name: get_schema_snapshot
  type: http
  seq: 1

http:
  method: GET
  url: "{{base_url}}/schema/snapshot"
  params:
    - name: export
      value: "json"
      type: query
      disabled: true
  auth: inherit

settings:
  encodeUrl: true

docs: |-
  Retrieve the current schema snapshot of the Directus instance.
  https://directus.io/docs/api/schema
