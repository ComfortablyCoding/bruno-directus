info:
  name: delete_dashboards_bulk
  type: http
  seq: 8

http:
  method: DELETE
  url: "{{base_url}}/dashboards"
  body:
    type: json
    data: |-
      ["id-1", "id-2"]
  auth: inherit

settings:
  encodeUrl: true

docs: |-
  Delete multiple Insights dashboards by their primary keys.
  https://directus.io/docs/api/dashboards
