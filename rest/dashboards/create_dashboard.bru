meta {
  name: create_dashboard
  type: http
  seq: 3
}

post {
  url: {{base_url}}/dashboards
  body: json
  auth: inherit
}

body:json {
  {
    "name": "Analytics",
    "icon": "dashboard",
    "note": "Main analytics dashboard"
  }
}

docs {
  Create a new Insights dashboard.
  https://directus.io/docs/api/dashboards
}
