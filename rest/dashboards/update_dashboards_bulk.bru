meta {
  name: update_dashboards_bulk
  type: http
  seq: 6
}

patch {
  url: {{base_url}}/dashboards
  body: json
  auth: inherit
}

body:json {
  {
    "keys": ["id-1", "id-2"],
    "data": {"name": "Analytics v2"}
  }
}

docs {
  Update multiple Insights dashboards by their primary keys.
  https://directus.io/docs/api/dashboards
}
