info:
  name: create_version
  type: http
  seq: 3

http:
  method: POST
  url: "{{base_url}}/versions"
  body:
    type: json
    data: |-
      {
        "collection": "articles",
        "item": "item-id",
        "name": "Draft v2",
        "key": "draft-v2"
      }
  auth: inherit

settings:
  encodeUrl: true

docs: |-
  Create a new content version for an item.
  https://directus.io/docs/api/versions
