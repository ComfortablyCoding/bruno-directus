info:
  name: compare_version
  type: http
  seq: 9

http:
  method: GET
  url: "{{base_url}}/versions/:id/compare"
  params:
    - name: id
      value: "version-id"
      type: path
  auth: inherit

settings:
  encodeUrl: true

docs: |-
  Compare a content version against the current state of the main item to see what has changed.
  https://directus.io/docs/api/versions
