info:
  name: delete_permissions_bulk
  type: http
  seq: 9

http:
  method: DELETE
  url: "{{base_url}}/permissions"
  body:
    type: json
    data: |-
      ["id-1", "id-2"]
  auth: inherit

settings:
  encodeUrl: true

docs: |-
  Delete multiple permissions at once.
  https://directus.io/docs/api/permissions
