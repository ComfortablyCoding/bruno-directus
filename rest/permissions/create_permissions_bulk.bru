meta {
  name: create_permissions_bulk
  type: http
  seq: 5
}

post {
  url: {{base_url}}/permissions
  body: json
  auth: inherit
}

body:json {
  [
    {"collection": "articles", "action": "read", "policy": "policy-id"},
    {"collection": "articles", "action": "create", "policy": "policy-id"}
  ]
}

docs {
  Create multiple permissions at once.
  https://directus.io/docs/api/permissions
}
