info:
  name: update_permissions_bulk
  type: http
  seq: 7

http:
  method: PATCH
  url: "{{base_url}}/permissions"
  body:
    type: json
    data: |-
      {
        "keys": ["id-1", "id-2"],
        "data": {"fields": ["id", "title", "status"]}
      }
  auth: inherit

settings:
  encodeUrl: true

docs: |-
  Update multiple permissions at once.
  https://directus.io/docs/api/permissions
