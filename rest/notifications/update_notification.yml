info:
  name: update_notification
  type: http
  seq: 5

http:
  method: PATCH
  url: "{{base_url}}/notifications/:id"
  params:
    - name: id
      value: "notification-id"
      type: path
  body:
    type: json
    data: |-
      {
        "status": "archived"
      }
  auth: inherit

settings:
  encodeUrl: true

docs: |-
  Update a single notification by its primary key.
  https://directus.io/docs/api/notifications
