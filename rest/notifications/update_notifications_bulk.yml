info:
  name: update_notifications_bulk
  type: http
  seq: 6

http:
  method: PATCH
  url: "{{base_url}}/notifications"
  body:
    type: json
    data: |-
      {
        "keys": ["id-1", "id-2"],
        "data": {"status": "archived"}
      }
  auth: inherit

settings:
  encodeUrl: true

docs: |-
  Update multiple notifications by their primary keys.
  https://directus.io/docs/api/notifications
