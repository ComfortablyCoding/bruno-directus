info:
  name: delete_item
  type: http
  seq: 7

http:
  method: DELETE
  url: "{{base_url}}/items/:collection/:id"
  params:
    - name: collection
      value: "my_collection"
      type: path
    - name: id
      value: "item-id"
      type: path
  auth: inherit

settings:
  encodeUrl: true

docs: |-
  Delete a single item from the specified collection by its primary key.
  https://directus.io/docs/api/items
