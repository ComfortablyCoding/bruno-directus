meta {
  name: update_item
  type: http
  seq: 5
}

patch {
  url: {{base_url}}/items/:collection/:id
  body: json
  auth: inherit
}

params:path {
  collection: my_collection
  id: item-id
}

body:json {
  {
    "title": "Updated Item"
  }
}

docs {
  Update a single item in the specified collection by its primary key.
  https://directus.io/docs/api/items
}
