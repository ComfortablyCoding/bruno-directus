info:
  name: ping_server
  type: http
  seq: 3

http:
  method: GET
  url: "{{base_url}}/server/ping"
  auth: inherit

settings:
  encodeUrl: true

docs: |-
  Ping the server to verify it is reachable. Returns "pong".
  https://directus.io/docs/api/server
