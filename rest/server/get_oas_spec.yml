info:
  name: get_oas_spec
  type: http
  seq: 4

http:
  method: GET
  url: "{{base_url}}/server/specs/oas"
  auth: inherit

settings:
  encodeUrl: true

docs: |-
  Retrieve the OpenAPI specification for this Directus instance.
  https://directus.io/docs/api/server
