info:
  name: get_server_health
  type: http
  seq: 2

http:
  method: GET
  url: "{{base_url}}/server/health"
  auth: inherit

settings:
  encodeUrl: true

docs: |-
  Check the health status of the server and its connected services.
  https://directus.io/docs/api/server
