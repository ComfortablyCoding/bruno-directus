info:
  name: query_fields
  type: http
  seq: 1

http:
  method: GET
  url: "{{base_url}}/fields"
  auth: inherit

settings:
  encodeUrl: true

docs: |-
  Retrieve a list of all fields across all collections.
  https://directus.io/docs/api/fields
