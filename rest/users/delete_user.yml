info:
  name: delete_user
  type: http
  seq: 9

http:
  method: DELETE
  url: "{{base_url}}/users/:id"
  params:
    - name: id
      value: "user-id"
      type: path
  auth: inherit

settings:
  encodeUrl: true

docs: |-
  Delete a user account by their ID.
  https://directus.io/docs/api/users
