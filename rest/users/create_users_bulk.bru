meta {
  name: create_users_bulk
  type: http
  seq: 5
}

post {
  url: {{base_url}}/users
  body: json
  auth: inherit
}

body:json {
  [
    {"email": "user1@example.com", "password": "password123", "role": "role-id"},
    {"email": "user2@example.com", "password": "password123", "role": "role-id"}
  ]
}

docs {
  Create multiple user accounts in a single request.
  https://directus.io/docs/api/users
}
