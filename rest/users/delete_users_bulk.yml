info:
  name: delete_users_bulk
  type: http
  seq: 10

http:
  method: DELETE
  url: "{{base_url}}/users"
  body:
    type: json
    data: |-
      ["id-1", "id-2"]
  auth: inherit

settings:
  encodeUrl: true

docs: |-
  Delete multiple user accounts by their IDs in a single request.
  https://directus.io/docs/api/users
