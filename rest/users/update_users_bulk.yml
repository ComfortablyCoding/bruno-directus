info:
  name: update_users_bulk
  type: http
  seq: 8

http:
  method: PATCH
  url: "{{base_url}}/users"
  body:
    type: json
    data: |-
      {
        "keys": ["id-1", "id-2"],
        "data": {"role": "role-id"}
      }
  auth: inherit

settings:
  encodeUrl: true

docs: |-
  Update multiple user accounts in a single request.
  https://directus.io/docs/api/users
