info:
  name: create_panel
  type: http
  seq: 3

http:
  method: POST
  url: "{{base_url}}/panels"
  body:
    type: json
    data: |-
      {
        "dashboard": "dashboard-id",
        "name": "Article Count",
        "type": "metric",
        "position_x": 0,
        "position_y": 0,
        "width": 8,
        "height": 6,
        "options": {}
      }
  auth: inherit

settings:
  encodeUrl: true

docs: |-
  Create a new panel on an Insights dashboard.
  https://directus.io/docs/api/panels
