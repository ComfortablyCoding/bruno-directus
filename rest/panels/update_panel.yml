info:
  name: update_panel
  type: http
  seq: 5

http:
  method: PATCH
  url: "{{base_url}}/panels/:id"
  params:
    - name: id
      value: "panel-id"
      type: path
  body:
    type: json
    data: |-
      {
        "name": "Updated Panel",
        "width": 12
      }
  auth: inherit

settings:
  encodeUrl: true

docs: |-
  Update a single Insights dashboard panel by its primary key.
  https://directus.io/docs/api/panels
