meta {
  name: update_folder
  type: http
  seq: 5
}

patch {
  url: {{base_url}}/folders/:id
  body: json
  auth: inherit
}

params:path {
  id: folder-id
}

body:json {
  {
    "name": "Renamed Folder"
  }
}

docs {
  Update the name or parent of a single folder by its ID.
  https://directus.io/docs/api/folders
}
