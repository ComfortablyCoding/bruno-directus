info:
  name: create_folders_bulk
  type: http
  seq: 4

http:
  method: POST
  url: "{{base_url}}/folders"
  body:
    type: json
    data: |-
      [
        {"name": "Folder 1"},
        {"name": "Folder 2"}
      ]
  auth: inherit

settings:
  encodeUrl: true

docs: |-
  Create multiple folders in a single request.
  https://directus.io/docs/api/folders
