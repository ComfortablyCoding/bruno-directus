info:
  name: update_roles_bulk
  type: http
  seq: 6

http:
  method: PATCH
  url: "{{base_url}}/roles"
  body:
    type: json
    data: |-
      {
        "keys": ["id-1", "id-2"],
        "data": {"icon": "verified_user"}
      }
  auth: inherit

settings:
  encodeUrl: true

docs: |-
  Update multiple roles at once.
  https://directus.io/docs/api/roles
