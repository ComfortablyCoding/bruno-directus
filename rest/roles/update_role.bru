meta {
  name: update_role
  type: http
  seq: 5
}

patch {
  url: {{base_url}}/roles/:id
  body: json
  auth: inherit
}

params:path {
  id: role-id
}

body:json {
  {
    "name": "Senior Editor"
  }
}

docs {
  Update an existing role by its primary key.
  https://directus.io/docs/api/roles
}
