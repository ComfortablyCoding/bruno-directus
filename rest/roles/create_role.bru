meta {
  name: create_role
  type: http
  seq: 3
}

post {
  url: {{base_url}}/roles
  body: json
  auth: inherit
}

body:json {
  {
    "name": "Editor",
    "icon": "edit"
  }
}

docs {
  Create a new role in the system.
  https://directus.io/docs/api/roles
}
