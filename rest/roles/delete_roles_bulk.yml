info:
  name: delete_roles_bulk
  type: http
  seq: 8

http:
  method: DELETE
  url: "{{base_url}}/roles"
  body:
    type: json
    data: |-
      ["id-1", "id-2"]
  auth: inherit

settings:
  encodeUrl: true

docs: |-
  Delete multiple roles at once.
  https://directus.io/docs/api/roles
