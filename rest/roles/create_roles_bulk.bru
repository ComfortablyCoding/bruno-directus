meta {
  name: create_roles_bulk
  type: http
  seq: 4
}

post {
  url: {{base_url}}/roles
  body: json
  auth: inherit
}

body:json {
  [
    {"name": "Editor", "icon": "edit"},
    {"name": "Viewer", "icon": "visibility"}
  ]
}

docs {
  Create multiple roles at once.
  https://directus.io/docs/api/roles
}
