info:
  name: update_files_bulk
  type: http
  seq: 6

http:
  method: PATCH
  url: "{{base_url}}/files"
  body:
    type: json
    data: |-
      {
        "keys": ["id-1", "id-2"],
        "data": {"folder": "folder-id"}
      }
  auth: inherit

settings:
  encodeUrl: true

docs: |-
  Update metadata for multiple files in a single request.
  https://directus.io/docs/api/files
