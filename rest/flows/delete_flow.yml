info:
  name: delete_flow
  type: http
  seq: 7

http:
  method: DELETE
  url: "{{base_url}}/flows/:id"
  params:
    - name: id
      value: "flow-id"
      type: path
  auth: inherit

settings:
  encodeUrl: true

docs: |-
  Delete a single flow by its primary key.
  https://directus.io/docs/api/flows
