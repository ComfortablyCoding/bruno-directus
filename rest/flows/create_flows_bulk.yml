info:
  name: create_flows_bulk
  type: http
  seq: 4

http:
  method: POST
  url: "{{base_url}}/flows"
  body:
    type: json
    data: |-
      [
        {"name": "My Flow", "icon": "bolt", "status": "active", "trigger": "manual"},
        {"name": "My Flow 2", "icon": "bolt", "status": "active", "trigger": "manual"}
      ]
  auth: inherit

settings:
  encodeUrl: true

docs: |-
  Create multiple flows in a single request.
  https://directus.io/docs/api/flows
