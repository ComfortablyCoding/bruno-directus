info:
  name: create_flow
  type: http
  seq: 3

http:
  method: POST
  url: "{{base_url}}/flows"
  body:
    type: json
    data: |-
      {
        "name": "My Flow",
        "icon": "bolt",
        "status": "active",
        "trigger": "manual"
      }
  auth: inherit

settings:
  encodeUrl: true

docs: |-
  Create a new flow in the Directus instance.
  https://directus.io/docs/api/flows
